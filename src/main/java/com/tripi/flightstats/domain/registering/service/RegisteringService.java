package com.tripi.flightstats.domain.registering.service;

import org.springframework.data.repository.reactive.ReactiveCrudRepository;

public interface RegisteringService {
    // core service registering
    // call FS to register
    // FS push back notification
    // create zendesk ticket
    // Paging/Sorting/Searching
    // Detail
    // Approve/Reject
    // Push notification: Notice change
    // Update change
}
